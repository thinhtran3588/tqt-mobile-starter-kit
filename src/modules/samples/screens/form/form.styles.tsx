import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginLeft: 40,
    marginRight: 40,
    position: 'relative',
  },
  button: {
    marginTop: 20,
  },
  menuItem: {
    maxWidth: '100%',
  },
});
